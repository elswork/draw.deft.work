version: '3.8'

services:
  airpaint:
    build: .
    container_name: draw-deft-work
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - .:/usr/share/nginx/html:ro # Hot-reload local: sincroniza cambios en desarrollo
